<UserControl x:Class="StudyingController.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition  Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBarTray Background="White">
            <ToolBar>
                <Button Command="{Binding LogoutCommand}" Content="Вийти"/>
            </ToolBar>
            <ToolBar>
                <Button Margin="3" Content="Додати" x:Name="btnAdd" Click="btnAdd_Click">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="cmAdd">
                            <MenuItem Header="Інститут" />
                            <MenuItem Header="Факультет" />
                            <MenuItem Header="Кафедру" />
                            <MenuItem Header="Групу" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                
                <Button Margin="3" Content="Видалити" x:Name="btnRemove"/>
            </ToolBar>

            <ToolBar Visibility="{Binding Path=IsSaveable, Converter={StaticResource B2V}}">
                <Button Margin="3" Content="Зберегти" IsEnabled="{Binding CurrentWorkspace.CanSave}"/>
                <Button Margin="3" Content="Відмінити зміни" IsEnabled="{Binding CurrentWorkspace.CanSave}"/>
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" >
                <Expander ExpandDirection="Right" Margin="2,0,5,2" IsExpanded="True" Background="Beige">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="*" ></RowDefinition>
                            </Grid.RowDefinitions>
                            <Expander Header="User Info" IsExpanded="True" Background="Bisque">
                                <ContentControl Width="170" Height="130"></ContentControl>
                            </Expander>
                            <ItemsControl Grid.Row="1" Width="Auto" ItemsSource="{Binding MainCommands}" HorizontalContentAlignment="Stretch" Background="Beige">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <Button Margin="3,3,3,3" FontSize="12" HorizontalAlignment="Stretch" Content="{Binding Path=Name}" Command="{Binding Path=Command}"></Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>                        
                    </ScrollViewer>
                </Expander>
            </Grid>
            <ContentControl Grid.Column="1" Background="Azure" Margin="0,0,2,2" Content="{Binding CurrentWorkspace}"/>
            <!--<Grid Grid.Column="1">
                <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" HorizontalAlignment="Left" >
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <ItemsControl ItemsSource="{Binding PathCommands}" Width="Auto" >
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Margin="3,3,0,3" FontSize="12" HorizontalAlignment="Center" Content="{Binding Path=Name}" Command="{Binding Path=Command}"></Button>
                                        <TextBlock Text="->" Margin="0,3,0,3"></TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="{Binding CurrentWorkspace.HeaderText}" Visibility="{Binding Path=HasWorkspaces, Converter={StaticResource B2V}}"/>
                    </StackPanel>
                </ScrollViewer>
                
                
            </Grid>-->
        </Grid>
    </Grid>
</UserControl>
