<UserControl x:Class="StudyingController.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
             <!--:DesignHeight="Auto" d:DesignWidth="300"-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition  Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBarTray Background="White">
            <ToolBar  ToolTip="This toolbar for all users">
                <Button Width="30" Height="30" ToolTip="Logout">Out</Button>
                <Button Width="30" Height="30" ToolTip="Edit Information">EI</Button>
            </ToolBar>
            <ToolBar ToolTip="This toolbar must have modifications for all users" ItemsSource="{Binding ToolbarCommands}">
                <ToolBar.ItemTemplate>
                    <DataTemplate>
                        <Button Width="30" Height="30" Content="{Binding Path=Name}"></Button>
                    </DataTemplate>
                </ToolBar.ItemTemplate>
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" >
                <Expander ExpandDirection="Right" Margin="2,0,5,2" IsExpanded="True" Background="Beige">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="*" ></RowDefinition>
                            </Grid.RowDefinitions>
                            <Expander Header="User Info" IsExpanded="True" Background="Bisque">
                                <ContentControl Width="170" Height="130"></ContentControl>
                            </Expander>
                            <ItemsControl Grid.Row="1" Width="Auto" ItemsSource="{Binding VisibleCommands}" HorizontalContentAlignment="Stretch" Background="Beige">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <Button Margin="3,3,3,3" FontSize="12" HorizontalAlignment="Stretch" Content="{Binding Path=Name}" Command="{Binding Path=Command}"></Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Expander>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" HorizontalAlignment="Left" >
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <ItemsControl ItemsSource="{Binding PathCommands}" Width="Auto" >
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Margin="3,3,0,3" FontSize="12" HorizontalAlignment="Center" Content="{Binding Path=Name}" Command="{Binding Path=Command}"></Button>
                                        <TextBlock Text="->" Margin="0,3,0,3"></TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Content="current workspace"  MinWidth="75" Margin="3" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>
                <ContentControl Grid.Row="1" Grid.Column="1" Background="Azure" Margin="0,0,2,2" Content="{Binding CurrentWorkspace}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
